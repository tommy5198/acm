#include<cstdio>


int main(){
    int T;
    scanf("%d", &T);
    for(int seq=1; seq<=T; seq++) {
        int n, m;
        scanf("%d%d", &n, &m);
        for(int i=0; i<m; i++) {
            scanf("%d%d%d%d", &a, &b, &c, &d);
            e[b][a].push_back(d*10000+c);
            for(int j=0; j<e[d].size(); j++) {
                if()
            }
        }

    }
    return 0;
}
