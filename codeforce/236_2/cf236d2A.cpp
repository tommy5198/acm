#include<cstdio>

int main(){
    int k, a, b, v;
    int s, n, o, p;
    scanf("%d%d%d%d", &k, &a, &b, &v);
    s = a/v;
    s += a%v != 0;
    n = k * b / (k-1);
    if(s <= n){
        printf("%d\n", )
}
